import React from "react";
import { AppBar, Toolbar } from "@material-ui/core";

const InsertData = ()=>{
    return (
        <div>
            <AppBar>
                <Toolbar>
                    
                </Toolbar>
            </AppBar>
        </div>
    )
}

export {InsertData}