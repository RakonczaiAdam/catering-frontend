import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
    field: {
        marginTop : 20,
        marginBottom: 20,
        display: 'block',
        width: "70%"
    },
    selectField: {
        width: "70%"
    }
})

export { useStyle }